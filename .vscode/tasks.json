{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/DistributedGrapher.Hub/DistributedGrapher.Hub.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}